import { ShortPipePipe } from './short-pipe.pipe';

describe('ShortPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ShortPipePipe();
    expect(pipe).toBeTruthy();
  });
});
